apiVersion: v1
kind: Pod
metadata:
  name: valid-kyverno-pod
  labels:
    app.kubernetes.io/name: valid-kyverno-pod
    app.kubernetes.io/component: service
    app.kubernetes.io/owner: retail-store-sample
spec:
  securityContext:
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: app
      image: nginx:1.27.4
      securityContext:
        runAsNonRoot: true
        readOnlyRootFilesystem: true
        allowPrivilegeEscalation: false
        capabilities:
          drop:
            - ALL
      resources:
        requests:
          cpu: 50m
          memory: 64Mi
        limits:
          memory: 128Mi
